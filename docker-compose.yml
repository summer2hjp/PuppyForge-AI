version: '3.9'

services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_DB: puppyforge
      POSTGRES_USER: rebel
      POSTGRES_PASSWORD: rebelpuppy2026
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"